<% if (it.backlinks.length > 0) {%>
<h4>Backlinks</h4>
<ul id="backlinks-list">
  <% it.backlinks.forEach(link => { %>
  <li>
    <a href="/<%= link.slug %>"><%= link.title %></a>
  </li>
  <% }) %>
</ul>
<% } %>
<!-- vim:ft=html -->
