{
  "importMap": "import_map.json",
  "tasks": {
    "build": "deno run -A --unstable main.ts --input docs",
    "serve": "deno run -A --unstable main.ts --input docs --serve",
    "dev": "deno run -A --unstable --watch=assets/,views/ main.ts --input docs --serve --local",
    "clean": "rm -rf _site"
  }
}
